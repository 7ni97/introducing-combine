/// The text in this repository copies freely from the documentation.
/// Generally, these should be free standing Swift files which you can
/// directly execute from the command line:
///
///     $ swift 01-Cancellable.swift
///
/// However, you might find it more readable to paste them in an Xcode
/// Playground and execute them there (`⌘ + ⇧ + return`).

import Combine

/// Click on the import Combine above. The only import you'll see there is
///
///     import Darwin
///
/// Combine is closed source. But it has no other dependencies except the
/// platform itself.
